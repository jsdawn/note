{
  "name": "blog-vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "docs:dev": "cross-env VITE_BUILD_ENV=/ vitepress dev docs",
    "docs:build": "cross-env VITE_BUILD_ENV=/note/ vitepress build docs",
    "vercel:build": "cross-env VITE_BUILD_ENV=/ vitepress build docs",
    "docs:serve": "vitepress serve docs"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.0",
    "cross-env": "^7.0.3",
    "sass": "^1.54.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.0",
    "vitepress": "1.0.0-alpha.4",
    "vue": "^3.2.47",
    "vue-tsc": "^0.38.4"
  },
  "engines": {
    "node": ">14.0.0"
  }
}
